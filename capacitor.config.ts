import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sira.jerusalem',
  appName: 'سِيرة',
  webDir: 'dist',
  android: {
    allowMixedContent: false,
  },
};

export default config;
